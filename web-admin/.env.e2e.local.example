# Playwright e2e 测试凭证模版
# 复制为 .env.e2e.local（已 gitignore，不会提交），填入真实测试账号密码。
#
#   cp .env.e2e.local.example .env.e2e.local
#
# 必须使用专用账号 e2e_admin（不要用日常开发的 admin），原因见 tests/README.md。

E2E_ADMIN_PASSWORD=

# 可选：覆盖默认用户名（默认 e2e_admin）
# E2E_ADMIN_USERNAME=e2e_admin
